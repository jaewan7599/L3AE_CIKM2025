test:
  metrics: [recall, ndcg]
  k: [5, 10, 20, 50]
  batch_size: 1024
  save_result_file: false

train:
  reproducible: true
  seed: 2023
  trainer: LinearTrainer

data:
  type: linear_cf
  name: games

model:
  name: l3ae_collective
  # general parameters here
  reg: 1
  alpha: 1
